pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Maven version'
            }
        }
         stage('Maven ') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
