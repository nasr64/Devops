pipeline {
    agent {label 'agent1'}

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
         stage('Maven') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
